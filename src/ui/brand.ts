/** Bundled brand assets (served from /public). The chrome Σ mark and the
 *  ARTYMER wordmark. The company's uploaded logo overrides the mark when set. */
export const BRAND_MARK = "/brand/mark.png";
export const BRAND_WORDMARK = "/brand/wordmark.png";
